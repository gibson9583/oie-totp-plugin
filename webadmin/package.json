{
    "name": "oie-totp-mfa-webadmin",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "node build.mjs",
        "test": "node --test test/*.test.mjs",
        "test:browser": "node test/browser-regressions.mjs"
    },
    "devDependencies": {
        "esbuild": "^0.21.0"
    }
}
